import React, { Component } from 'react';
import API from '../../utils/API';

function List(props) {
    const info = (props.sortby === 'firstname') ? 'first' : 'last';
      return (
        <div>

        </div>
      );
  }  


export default Search;